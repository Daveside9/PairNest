# render.yaml
services:
  - type: web
    name: pairnest-frontend
    env: static
    staticPublishPath: build
    buildCommand: npm install && npm run build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
